import React from 'react'
import { Button } from './style'

export const InputSendOrder = props => {

    return (
        <Button desebled={ false } >
            Complete order
        </Button>
    )
}